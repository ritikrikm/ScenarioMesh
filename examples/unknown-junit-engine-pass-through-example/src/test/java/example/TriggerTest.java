package example;

import org.junit.platform.commons.annotation.Testable;

/** Marker class matching Surefire's default scan; the custom engine owns its execution semantics. */
@Testable
public final class TriggerTest {
}
